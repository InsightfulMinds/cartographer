# reference-shelf
status: live
source: ops/reference/ (CHECKS.md, hard-rules.md, PACKET-STANDARD.md, devices.md, fleet-paths.json, STATE.md, sops/) ; STATE.md generated by bin/state-probe.sh via com.fleet.state-probe.plist (StartInterval 3600)
what: The protected shelf of canon a session reads before acting: the named-check registry
  (resolve a phrase to ONE check with an evidence artifact), the hard rules, packet and
  contact standards, the fleet path map. One file is GENERATED: STATE.md is rewritten
  hourly from live port probes + launchctl and must never be hand-edited.
moves: state-probe hourly → overwrite STATE.md (last write 14:00 on 08-29); everything
  else changes by human edit under write-guard's protected tier.
hits: every session-start read; index-router aliases that point into this shelf.
does-not-hit: the session tool's own instruction file at the runtime root — that is
  operator instruction, not reference canon; editing CHECKS.md changes no session prompt.
